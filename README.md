# Rafter Example: Node.js

This is an example of a Node.js project running on rafter.

Note the use of `process.env.PORT` to ensure the app listens for the correct port, provided by Google Cloud Run.
